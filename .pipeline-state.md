# パイプライン状態

- **project:** kenko-kanri
- **feature:** sanpo-ban（v3 + UI hotfix + リファクタ）
- **パイプライン:** v3 **受入完了** — UI hotfix **人間承認済** — リファクタ **実施済** — グラフ hotfix **実装済**（2026-06-16）
- **ブランチ:** `main`（`develop` へのマージは人間が実施）
- **最終更新:** 2026-06-16

## グラフ hotfix（2026-06-16）

- 未記録日の収支を null に（LBM のみ同期済みでも BMR+NEAT だけの架空値を出さない）
- 履歴 API: 日次 metric をキャッシュ完結（DB 逐次クエリ削除）
- 収支グラフ: ゼロ基準の棒グラフ（マイナスは下方向）
- 期間タブ: 4 期間を並列 prefetch + DOM 部分更新で即切替
- 線グラフ: ソース上は既に削除済み（Pi 再デプロイで反映）

## 段階

| 段階 | 状態 |
|------|------|
| 設計〜契約 | 承認済（コード準拠でドキュメント更新済） |
| 実装 v3 | 完了 |
| verify-run | 合格 |
| review-code | 合格 |
| 受入 v3 | **合格** |
| UI hotfix（食事・運動・設定） | **人間承認済** |
| リファクタ・ドキュメント整合 | **完了** |

## リファクタ内容（2026-06-15）

### コード
- Backend: v1 未使用計算関数削除、`dashboard_service` デッドヘルパー削除
- Backend: `user_profile.stride_cm` / `walking_speed_kmh` 削除（migration `007`）。歩行パラメータは `daily_steps` のみ
- Backend: 運動ログ日付範囲クエリ共通化、sanpo 表記更新
- Frontend: 未使用 API（`getWeights`/`deleteWeight`）、`formatValue`、旧 CSS 削除
- Frontend: `MEAL_SLOT_LABELS` 共通化、`package-lock` 名修正

### ドキュメント（コード正）
- `docs/README.md`: v3 完了、NEAT 初期 180、`meal_slot` / Health sync 追記
- `contract/openapi.yaml` / `contract/types.ts`: `meal_slot`、DashboardCards 歩行フィールド、Health sync 歩行パラメータ
- `detailed-design/v3-balance-ui.md`: NEAT 180、設定・API・migration 005–007

## Pi デプロイ（人間）

```bash
# MySQL（初回のみ）
DROP DATABASE IF EXISTS sanpo_ban;
CREATE DATABASE kenko_kanri CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
DROP USER IF EXISTS 'sanpo'@'localhost';
CREATE USER 'kenko'@'localhost' IDENTIFIED BY 'kenko';
GRANT ALL ON kenko_kanri.* TO 'kenko'@'localhost';
FLUSH PRIVILEGES;

# アプリ
cd ~/kenko-kanri
git pull
cd src/backend
# .env の DATABASE_URL を mysql+pymysql://kenko:kenko@localhost:3306/kenko_kanri に更新
uv run alembic upgrade head   # 007 まで適用
cd ../frontend && npm run build
sudo systemctl restart kenko-kanri
```

### デプロイ後の確認

- [ ] 設定タブで初回セットアップ
- [ ] Health ショートカット → LBM・歩数同期
- [ ] 食事: 各枠の「入力」で正しいセクションに記録される
- [ ] Myセット: 追加・削除
- [ ] 運動: 過去日のトレッドミル・筋トレ記録
- [ ] TOP 収支・履歴を目視確認

## 歩数 kcal（決定: 案 C）

- **歩数 → 屋外**（MET 式）。**NEAT → 屋内のみ**（デフォルト 180 kcal、設定で変更）
- 計算: `kcal = MET(速度) × 体重kg × 歩行時間h`。距離 = 歩数 × 歩幅
- 歩幅・速度は **Health sync → `daily_steps` のみ**（profile には保存しない）
- migrations: `005_walk_met_params`, `006_meal_slot`, `007_drop_profile_walk_params`

## メトリクス

| 項目 | 値 |
|------|-----|
| 人間差し戻し回数 | — |
| 自動差し戻し回数 | — |
| hotfix 承認日 | 2026-06-15 |

## 次のアクション

**人間:** Pi へ再デプロイ（`npm run build` + restart）してグラフ hotfix を確認。問題なければ `fix:` コミットまたは `main` → `develop` マージ。
