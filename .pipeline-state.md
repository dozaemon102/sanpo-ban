# パイプライン状態

- **project:** kenko-kanri（現フォルダ `sanpo-ban`）
- **パイプライン:** v3 **受入完了（条件付き）** — 人間 Pi デプロイ待ち
- **最終更新:** 2026-06-15

## 段階

| 段階 | 状態 |
|------|------|
| 設計〜契約 | 承認済 |
| 実装 v3 | 完了 |
| verify-run | 条件付き合格 |
| review-code | 条件付き合格 |
| 受入 v3 | **条件付き合格**（人間 Pi 確認待ち） |
| UI hotfix（食事・運動タブ） | **実装済**（人間確認待ち） |

## 直近変更（2026-06-15）

- `ui.pptx` 参照で食事・運動タブをセクション型 UI に刷新
  - 食事: 日付ストリップ、朝食/昼食/夕食/間食セクション、各セクションにバーコード
  - 運動: 消費合計・ヘルスケア歩数、トレッドミル/筋トレセクション（記録フォーム折りたたみ）
- 散歩 UI は引き続きなし（pptx 指示どおり）

## Pi デプロイ（人間）

```bash
# MySQL
DROP DATABASE IF EXISTS sanpo_ban;
CREATE DATABASE kenko_kanri CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
DROP USER IF EXISTS 'sanpo'@'localhost';
CREATE USER 'kenko'@'localhost' IDENTIFIED BY 'kenko';
GRANT ALL ON kenko_kanri.* TO 'kenko'@'localhost';
FLUSH PRIVILEGES;

# アプリ
cd ~/kenko-kanri/src/backend
# .env の DATABASE_URL を mysql+pymysql://kenko:kenko@localhost:3306/kenko_kanri に更新
uv run alembic upgrade head
cd ../frontend && npm run build
sudo systemctl restart kenko-kanri
```

- 設定タブで初回セットアップ
- Health ショートカット → LBM 同期
- TOP 収支・履歴・食事タブを目視確認
