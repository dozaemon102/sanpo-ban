# パイプライン状態

- **project:** kenko-kanri（現フォルダ `sanpo-ban`）
- **パイプライン:** v3 **受入完了（条件付き）** — 人間 Pi デプロイ待ち
- **最終更新:** 2026-06-14

## 段階

| 段階 | 状態 |
|------|------|
| 設計〜契約 | 承認済 |
| 実装 v3 | 完了 |
| verify-run | 条件付き合格 |
| review-code | 条件付き合格 |
| 受入 v3 | **条件付き合格**（人間 Pi 確認待ち） |

## Pi デプロイ（人間）

```bash
# MySQL
DROP DATABASE IF EXISTS sanpo_ban;
CREATE DATABASE kenko_kanri CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# アプリ
cd ~/kenko-kanri/src/backend   # または sanpo-ban
uv run alembic upgrade head
cd ../frontend && npm run build
sudo systemctl restart kenko-kanri
```

- 設定タブで初回セットアップ
- Health ショートカット → LBM 同期
- TOP 収支・履歴・食事タブを目視確認
