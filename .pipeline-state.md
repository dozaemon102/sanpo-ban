# パイプライン状態

- **project:** kenko-kanri
- **feature:** sanpo-ban（v3 + hotfix 継続）
- **モード:** **hotfix**（グラフ・日次 log 整合 — 詳細設計差分・受入は**いったん省略**、人間承認 2026-06-16）
- **パイプライン:** v3 **受入完了** — 以降は `fix:` コミットを `main` で継続
- **ブランチ:** `main`（`develop` へのマージは人間が実施）
- **最終更新:** 2026-06-16

## hotfix: グラフ・log_date（2026-06-16）

### フロント
- 収支グラフ: ゼロ基準棒、全マイナス時 Y 軸、X 軸ラベル重なり解消
- 期間タブ: metric 別キャッシュ + 4 期間 prefetch
- 履歴: **実測・記録があった日のみ**表示

### バックエンド
- `weight_logs`: **`log_date`（JST）1 日 1 行**（migration `008`、既存 weight データ drop）
- Health / 設定 / manual: **送った項目だけ** upsert（同日後勝ち）
- `treadmill_logs` / `strength_logs`: **`log_date` 列追加**（migration `009`、1 日複数行可）
- migration `009` MySQL 向け修正（`alter_column` に `existing_type`、途中失敗から再実行可）
- 履歴 API: 日次集計を `log_date` ベースに統一

### コミット（main）
- `d8aa249` … グラフ hotfix
- `6d68ec8` … weight / exercise log_date
- `82615a6` … migration 009 MySQL 修正

## 段階

| 段階 | 状態 |
|------|------|
| 設計〜契約 | 承認済（v3） |
| 受入 v3 | **合格** |
| hotfix（グラフ・log_date） | **人間承認済**（fix でクローズ、設計書追記は後回し可） |

## Pi デプロイ（人間）

```bash
cd ~/kenko-kanri && git pull
cd src/backend && uv run alembic upgrade head   # 008, 009
cd ../frontend && npm run build
sudo systemctl restart kenko-kanri
```

**009 が途中失敗している場合:** 修正版 pull 後に `alembic upgrade head` を再実行（列追加済みでも再開可）。

### デプロイ後の確認

- [ ] 体組成のみ Health sync → LBM グラフに出る
- [ ] 同日 manual 体重 → 体組成は残る
- [ ] 運動（トレッドミル・筋トレ）過去日記録 → 履歴 exercise に出る
- [ ] 収支グラフ: 未記録日 null、記録日のみ棒表示
- [ ] 期間タブ切替が速い（metric 別キャッシュ）

## migrations

`005`–`007`（v3）, `008_weight_log_date`, `009_exercise_log_date`

## メトリクス

| 項目 | 値 |
|------|-----|
| hotfix 承認日（UI） | 2026-06-15 |
| hotfix 承認日（グラフ・log_date） | 2026-06-16 |

## 次のアクション

**人間:** Pi で上記デプロイ・確認。問題なければ hotfix クローズ（`main` → `develop` マージは任意・タイミングは人間判断）。

**後日（任意）:** `detailed-design` / OpenAPI に log_date upsert を追記してから正式受入に載せ替え可能。
