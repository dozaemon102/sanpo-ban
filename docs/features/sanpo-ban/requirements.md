# 要件定義書: 散歩判（sanpo-ban）

## 1. 概要

**散歩判**は、個人向けのダイエット情報ダッシュボードである。食事（カロリー・PFC）、体重、歩数（iPhone 自動同期）、散歩記録、トレッドミル、筋トレを一か所に集約し、今日の摂取余力と運動状況を把握できるようにする。

家計簿型アプリのような毎食の詳細入力負荷は避け、定番プリセット・最小限の手入力・**バーコード読取（v2）**でガチダイエットに必要な数値を追える設計とする。認証・認可は行わず、開発者本人が自宅 LAN / Tailscale 内で利用する。

**v2（2026-06）:** Open Food Facts 連携によるバーコード食事記録を追加する。

## 2. 背景・目的

### 背景

- ダイエットを本気で行いたいが、記録をしていない（入力がだるい）
- 散歩はダイエット目的と、街での発見の楽しみの両方がある
- 食事で「食べていいか」迷うことがある
- ジムではトレッドミル・筋トレも行う
- 既存の汎用アプリ（家計簿・単機能歩数アプリ）は、情報が分散する

### 目的

- ダイエットに必要な情報を単一サービスに集約する
- 歩数は iPhone から自動取り込みし、手入力負荷を減らす
- PC 上でプロトタイプを完成させ、のちに Raspberry Pi（USB SSD）へ移行できるようにする

## 3. スコープ

### 3.1 対象（含む）

- Web ブラウザ UI（スマホ優先）
- REST API + MySQL によるデータ永続化
- 初期設定（身体情報・目標 kcal / PFC）
- 今日ダッシュボード（摂取・消費・残り kcal / PFC）
- 食事記録（プリセット・手入力）
- 体重記録（アプリ内入力 + iPhone 同期）
- 歩数の自動同期（iPhone ショートカット → API）
- 散歩セッション記録（1 タップ + 任意の発見メモ）
- トレッドミル記録（手入力）
- 筋トレ記録（クイック：種目テンプレ + 分数）
- 週次サマリー（摂取・歩数・体重・運動回数）
- PC ローカル開発環境（Docker Compose 想定）
- **v2:** バーコード読取（スマホカメラ）+ Open Food Facts 連携による食事記録
- **v1.1:** 食事・散歩・運動・体重の誤記録削除（UI）

### 3.2 対象外（含まない）

- ユーザー登録・ログイン・OAuth・外部 IdP
- 会社業務・GitHub / Cursor 連携
- クラウド SaaS ホスティング（初期リリース）
- Fitbit / Garmin 等クラウド API 連携
- Instagram / Twitter 自動取込
- ゲーム関連機能
- **v2 で対象外:** 食べる判 ○△×（FR-016）、筋トレ詳細（FR-032）、旅モード（FR-034/035）
- Raspberry Pi 本番デプロイ自動化（Pi 手順は backlog / pi-native で提供済）

## 4. ステークホルダー・ユーザー

| 区分 | 説明 |
|------|------|
| 主要ユーザー | 開発者本人（会社員 SE）。個人のダイエット管理のみ |
| その他 | なし（マルチユーザー非対応） |

## 5. ロール・権限

該当なし（全利用者同一権限）。LAN 内の単一利用者を前提とし、認証・認可機能は提供しない。

### 5.1 ロール定義

| ロール | 説明 |
|--------|------|
| ROLE-001（利用者） | 本人のみ。全機能を利用可能 |

### 5.2 権限マトリクス

| 操作 / リソース | ROLE-001 |
|----------------|----------|
| FR-001〜FR-039 | ○ |

## 6. 機能要件

### 6.1 初期設定・目標

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-001 | 利用者は身長・生年月日・性別・体重を登録できる | 必須 |
| FR-002 | システムは身体情報から目標摂取カロリー（kcal）を自動提案する | 必須 |
| FR-003 | 利用者は目標 kcal および PFC（グラムまたは比率）を確認・上書きできる | 必須 |
| FR-004 | 初期設定は初回利用時および設定画面から変更できる | 必須 |

### 6.2 今日ダッシュボード

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-005 | 利用者は当日の摂取 kcal・P/F/C 合計と目標に対する残量を確認できる | 必須 |
| FR-006 | 利用者は当日の推定消費 kcal（歩数・トレッドミル・筋トレの合算）を確認できる | 必須 |
| FR-007 | 利用者は当日の歩数（最新同期値）を確認できる | 必須 |
| FR-008 | 利用者は当日の最新体重を確認できる | 必須 |
| FR-009 | ダッシュボードから食事・運動・体重・散歩の各入力画面へ遷移できる | 必須 |

### 6.3 食事

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-010 | 利用者はよく食べる食品をプリセット（名称・kcal・P/F/C）として登録できる | 必須 |
| FR-011 | 利用者はプリセットを 1 操作で当日の食事に追加できる | 必須 |
| FR-012 | 利用者は名称・kcal・P/F/C を手入力して食事を記録できる | 必須 |
| FR-013 | 利用者は記録した食事を当日一覧で確認・削除できる | 必須 |
| FR-014 | 利用者は同一食事を別日に複製して記録できる | 任意 |
| FR-015 | 利用者はバーコードで Open Food Facts から食品情報を取得して食事に追加できる | **必須（v2）** |
| FR-016 | 利用者は食品ごとに食べる判（○/△/×）ルールを設定し、残り kcal と連動した判定を受けられる | 将来 |
| FR-037 | 利用者はスマホブラウザのカメラで JAN/EAN バーコードを読み取れる | 必須（v2） |
| FR-038 | 利用者は取得した商品情報（名称・kcal・P/F/C）を確認してから当日の食事に追加できる | 必須（v2） |
| FR-039 | Open Food Facts に商品がない、または API が失敗した場合、手入力画面へフォールバックして食事を記録できる | 必須（v2） |

### 6.4 体重

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-017 | 利用者はアプリ内で体重（kg）を 1 日 1 回以上記録できる | 必須 |
| FR-018 | システムは iPhone ショートカット経由で送信された体重を受け取り、当日記録として保存できる | 必須 |
| FR-019 | 利用者は体重の履歴（一覧または推移）を確認できる | 必須 |

### 6.5 歩数・Health 同期

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-020 | システムは iPhone ショートカットから POST された日次歩数を受け取り、該当日の最新値として保存する | 必須 |
| FR-021 | システムは同期リクエストに含まれる体重を FR-018 と同様に処理できる | 必須 |
| FR-022 | システムは受け取った歩数から体重連動の推定消費 kcal を算出し、当日消費に加算する | 必須 |
| FR-023 | 歩数同期 API は認証なしで LAN 内から呼び出せる（セキュリティは NFR で定義） | 必須 |

### 6.6 散歩

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-024 | 利用者は「散歩した」を 1 タップで散歩セッションとして記録できる | 必須 |
| FR-025 | 利用者は散歩セッションに任意で発見メモ（テキスト）を付与できる | 必須 |
| FR-026 | 利用者は散歩セッションの履歴を日付順で確認できる | 必須 |
| FR-036 | 利用者は誤記録した食事・散歩・トレッドミル・筋トレ・体重を削除できる | 必須 |

### 6.7 トレッドミル

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-027 | 利用者はトレッドミル運動を分数で記録できる | 必須 |
| FR-028 | 利用者は任意で速度（km/h）・傾斜（%）またはマシン表示 kcal を入力できる | 必須 |
| FR-029 | システムはトレッドミル記録から推定消費 kcal を算出し、当日消費に加算する（マシン kcal 直接入力時はその値を優先） | 必須 |

### 6.8 筋トレ

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-030 | 利用者は種目テンプレ（例：胸・背・脚・全身）と分数で筋トレを記録できる | 必須 |
| FR-031 | システムは筋トレ記録から推定消費 kcal を算出し、当日消費に加算する | 必須 |
| FR-032 | 利用者は種目・重量・セット×回数で筋トレを詳細記録できる | 将来 |

### 6.9 週次サマリー

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-033 | 利用者は直近 7 日間の平均摂取 kcal・平均歩数・体重推移・散歩/筋トレ/トレッドミル回数を確認できる | 必須 |

### 6.10 将来（スコープ外だが backlog 参照）

| ID | 要件 | 優先度 |
|----|------|--------|
| FR-034 | 利用者は旅先モードで行き先別の食事プリセット・発見メモを管理できる | 将来 |
| FR-035 | 利用者は Instagram 投稿用タグメモを散歩・旅記録に紐づけられる | 将来 |

## 7. 非機能要件

| ID | カテゴリ | 要件 |
|----|----------|------|
| NFR-001 | 性能 | 主要画面（今日ダッシュボード）の API 応答は通常時 1 秒以内 |
| NFR-002 | セキュリティ（認証・認可） | ユーザー認証・認可機能は提供しない。サービスは自宅 LAN 内での利用を前提とする |
| NFR-003 | セキュリティ（データ） | 身体・体重データは MySQL に保存し、外部 SaaS へ送信しない（Open Food Facts 呼出時を除く） |
| NFR-004 | 可用性・運用 | Phase 1 は PC（Docker Compose）で開発・動作確認できること |
| NFR-005 | 可用性・運用 | MySQL スキーマと設定は Pi 移行時に変更なしで持ち込めること |
| NFR-006 | 運用（Pi） | Pi 本番時は MySQL データディレクトリを USB SSD 上に配置できること（手順は infra で文書化） |
| NFR-007 | 保守性 | API は OpenAPI で契約化する（Phase 4.5） |
| NFR-008 | 使い勝手 | 食事の定番追加は 2 タップ以内、散歩記録は 1 タップ以内 |
| NFR-009 | 性能（外部 API） | Open Food Facts 参照は通常時 5 秒以内に結果またはエラーを返す |
| NFR-010 | 可用性（v2） | Pi 本番環境からインターネット経由で Open Food Facts API に到達できること |

## 8. 制約・前提条件

### 技術的制約

- DB は MySQL 8.x を使用する
- 進め方 A: Phase 1 の完成・検証は PC ローカルで行う
- iPhone 歩数・体重同期は iOS ショートカットによる HTTP POST に依存する
- **v2:** Open Food Facts API を Pi（バックエンド）から呼び出す。商品バーコードは EAN-13 / JAN を想定（詳細は基本設計）
- **v1.1 済:** 記録削除 UI。インライン編集は v2 スコープ外（削除＋再記録で対応）

### ビジネス制約

- 個人利用のみ。マルチテナント・課金・ストア公開は対象外

### 前提条件

- 利用者は iPhone を持ち、散歩時に携行する
- トレッドミル中はスマホを携行しない（歩数にジム走行分は含まれず、二重計上は発生しない）
- 利用者は自宅 Wi-Fi または Tailscale 上で Pi にアクセスできる
- 利用者が iOS ショートカットを設定できる（設定手順は README / 運用ドキュメントで提供）

## 9. 受入条件

| ID | 条件 | 確認方法 |
|----|------|----------|
| AC-001 | 初期設定完了後、今日ダッシュボードに目標 kcal と PFC 目標が表示される | 手動 UI 確認 |
| AC-002 | プリセット食事を 1 件追加したとき、摂取 kcal・P/F/C 合計が増加する | API + UI 確認 |
| AC-003 | 手入力食事を追加・削除したとき、合計が正しく増減する | API + UI 確認 |
| AC-004 | ショートカット POST で歩数 8000 を送信したとき、当日歩数が 8000 と表示される | curl / ショートカット実機 |
| AC-005 | 同一日に歩数 8000 の後 9000 を POST したとき、表示は 9000（最新値）である | API 確認 |
| AC-006 | ショートカット POST に weight_kg を含めたとき、当日体重が更新される | API 確認 |
| AC-007 | 「散歩した」記録後、散歩履歴に当該セッションが表示される | UI 確認 |
| AC-008 | 発見メモ付き散歩を記録したとき、履歴にメモが表示される | UI 確認 |
| AC-009 | トレッドミル 30 分を記録したとき、当日消費 kcal が 0 より大きく増加する | UI 確認 |
| AC-010 | 筋トレ（種目 + 45 分）を記録したとき、当日消費 kcal が増加する | UI 確認 |
| AC-011 | 体重を手入力したとき、ダッシュボードと履歴に反映される | UI 確認 |
| AC-012 | 7 日分のデータがあるとき、週サマリーに平均摂取・平均歩数が表示される | テストデータ + UI |
| AC-013 | Docker Compose 起動後、ブラウザから今日ダッシュボードにアクセスできる | verify-run（Pi ネイティブで代替可） |
| AC-014 | 有効な JAN/EAN バーコードをスキャンしたとき、商品名と kcal・P/F/C が確認画面に表示される | UI + API モック |
| AC-015 | 確認画面で追加したとき、当日の摂取 kcal・P/F/C 合計が増加する | UI 確認 |
| AC-016 | Open Food Facts に商品がないバーコードのとき、手入力フォームへ遷移できる | UI 確認 |
| AC-017 | バーコードから食事を追加したとき、食品プリセット件数は増えない | DB / UI 確認 |
| AC-018 | 食事・散歩・運動・体重の誤記録を UI から削除したとき、ダッシュボード集計から除外される | UI 確認 |

## 10. 用語定義

| 用語 | 定義 |
|------|------|
| PFC | タンパク質（Protein）・脂質（Fat）・炭水化物（Carbohydrate） |
| プリセット | 利用者が事前登録した定番食品（名称・kcal・P/F/C） |
| 散歩セッション | 「散歩した」操作で記録されるイベント。発見メモを任意で付与 |
| Health 同期 | iPhone ショートカットが API へ歩数・体重を POST すること |
| 推定消費 kcal | 歩数・トレッドミル・筋トレから算出した運動消費カロリー |
| Open Food Facts | OSS の食品データベース。バーコードから栄養成分を取得（v2 で利用） |
| バーコード読取 | スマホブラウザのカメラで JAN/EAN を読み取り、サーバーが OFF を参照する |

## 11. 未決事項

| ID | 内容 | 影響範囲 |
|----|------|----------|
| OPN-001 | 歩数→kcal 換算式の具体係数 | 基本設計（消費カロリー計算） |
| OPN-002 | トレッドミル実施中の iPhone 歩数を消費計算に含めるか | **解消** — ジム中スマホ非携行のため二重計上なし。歩数控除ロジック不要 |
| OPN-003 | TDEE 自動提案に用いる活動係数のデフォルト | 基本設計（FR-002） |
| OPN-004 | 筋トレ種目テンプレの初期一覧 | 基本設計 |
| OPN-005 | Pi 移行時の MySQL バージョン pin | infra / devops |
| OPN-006 | OFF レスポンスから kcal/PFC を算出するフィールド優先順位 | 基本設計（FR-015） |
| OPN-007 | カメラ権限拒否時の UX（手入力のみ案内等） | 基本設計（FR-037） |

## 変更履歴

| 日付 | 変更内容 |
|------|----------|
| 2026-06-13 | 初版作成（MySQL・進め方 A・iPhone 同期・PFC 要件を反映） |
| 2026-06-13 | 前提追加: トレッドミル中はスマホ非携行（OPN-002 解消） |
| 2026-06-13 | **v2:** FR-015/037〜039 必須化、バーコード AC 追加。FR-036 記録削除。v1.1 hotfix 反映 |
